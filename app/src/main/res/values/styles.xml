<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorSurface">@color/scroll</item>
    </style>

    <style name="home_page_buttons" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/colorOnPrimary</item>
    </style>

    <style name="pageHeaders">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textSize">@dimen/text_header</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:padding">@dimen/text_padding</item>
    </style>

    <style name="standard_text">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textSize">@dimen/text_body</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:padding">@dimen/text_padding</item>
        <item name="android:background">@color/scroll</item>
        <item name="strokeWidth">5dp</item>
        <item name="strokeColor">@color/colorAccent</item>

    </style>

</resources>
